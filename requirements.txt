# Python runtime dependencies for the COLMAG project.
#
# Curated from the local letter_recog_arm conda environment
# (Python 3.10.20, inspected 2026-06-03) plus the optional ROS bridge
# dependency used by magnetometer_reader.py on machines without rospy.
#
# This is intentionally not a full pip freeze: the conda environment also
# contains unrelated or platform-specific packages such as tensorflow-macos.
# The ranges below keep the file usable on laptops and inside ROS Noetic
# Docker images, whose system Python is commonly 3.8.

argcomplete>=3.1,<4
easyocr==1.7.2
matplotlib>=3.7,<3.11
numpy>=1.24,<3
opencv-python-headless>=4.8,<5
pandas>=2.0,<2.3
pillow>=10,<13
pyserial==3.5
roslibpy>=1.7,<2
scikit-image>=0.21,<0.26
scikit-learn>=1.3,<1.8
